apiVersion: v1
kind: ConfigMap
metadata:
  namespace: production
  name: mf-django-config
data:
  DB_NAME: services
  DB_PORT: '5432'
  DB_HOST: rc-psql-postgresql.release
  RABBITMQ_SVC: rc-rbmq-rabbitmq.release:5672
